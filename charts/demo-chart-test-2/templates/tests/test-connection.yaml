apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-chart-test-2.fullname" . }}-test-connection"
  labels:
    {{- include "demo-chart-test-2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-chart-test-2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
